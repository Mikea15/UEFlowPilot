---
title: UTagActorComponent
description: UFlowPilot reference page
---

import Ref from '../../../components/Ref.astro' 

<Ref c="UFlowPilotSettings" p="UDeveloperSettings" />

### Description

`UTagActorComponent` is a helper component that adds `FGameplayTag`'s to an Actor. 
You can add the tag in `BeingPlay`, or by default in `InitializeComponent`.
